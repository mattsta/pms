#!/usr/bin/env bash

# Backward-compatible wrapper for isolated real-world scenario helpers.

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
source "$ROOT_DIR/scripts/lib/run_helpers.sh"
